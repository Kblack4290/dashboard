export interface WatchlistItem {
  id: number;
  symbol: string;
  name: string;
  latestPrice: string;
  previousClose: string;
  dayRange: string;
  volume: string;
  changePercentage: string;
  dateAdded: Date;
}
